# Vendor Order (`/api/vendor-order/[orderToken]/`)
Auth: `orderToken` in path.

GET (poll) · POST `/submit` · POST `/revise-lyrics` · POST `/approve-lyrics` · POST `/generate-template` · POST `/rj-show/approve`
