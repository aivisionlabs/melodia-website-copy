# Partner API (`/api/v1/partner/`)
Auth: `Authorization: Bearer <api_key>` — hashed lookup in `partner_api_credentials`

GET `/templates` · POST `/orders` · GET `/orders/[id]` · POST `/orders/[id]/simulate` · POST `/orders/[id]/retry` · POST `/orders/[id]/approve-script`
